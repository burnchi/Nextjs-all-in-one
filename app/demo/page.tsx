import React from 'react'

const page = () => {
  // const array = [...Array(5)].map((_, i) => i)
  const array = [...Array(5)]
  console.log(array);

  return (
    <div>page</div>
  )
}

export default page
